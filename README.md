# Hello World!
### sql语句
```mysql
create table practice.student
(
    id       int auto_increment
        primary key,
    username varchar(255) null,
    age      int          null comment '年龄'
);

create table practice2.student
(
    id       int auto_increment
        primary key,
    username varchar(255) null,
    age      int          null comment '年龄'
);


```